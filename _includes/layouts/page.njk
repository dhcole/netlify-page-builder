---
layout: layouts/base.njk
section: page
---
<h1>{{ title }}</h1>

{% for section in sections %}
  {% include "components/sections/" + section.type + ".njk" %}
{% endfor %}

{{ layoutContent | safe }}
